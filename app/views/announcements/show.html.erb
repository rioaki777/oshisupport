<div class="flex-wrapper">
  <div class="content-wrapper">
    <div class="flex justify-center">
      <div class="text-center text-gray-500">
        <%= heroicon "bell", options: { class: "h-30 w-30" } %>
        <p class="text-4xl">お知らせ</p>
      </div>
    </div>
    <div class="m-5">
      <p class="text-2xl"><%= @announcement.title %></p>
      <p><%= l(@announcement.published_at, format: :short) %></p>
      <p class="mt-5"><%= simple_format(@announcement.description) %></p>
    </div>
  </div>
</div>
